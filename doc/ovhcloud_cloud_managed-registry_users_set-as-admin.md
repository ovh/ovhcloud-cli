## ovhcloud cloud managed-registry users set-as-admin

Set a specific container registry user as admin

```
ovhcloud cloud managed-registry users set-as-admin <registry_id> <user_id> [flags]
```

### Options

```
  -h, --help   help for set-as-admin
```

### Options inherited from parent commands

```
      --cloud-project string   Cloud project ID
  -d, --debug                  Activate debug mode (will log all HTTP requests details)
  -e, --ignore-errors          Ignore errors in API calls when it is not fatal to the execution
  -o, --output string          Output format: json, yaml, interactive, or a custom format expression. Run 'ovhcloud --help' for the full list with examples.
      --profile string         Use a specific profile from the configuration file
```

### SEE ALSO

* [ovhcloud cloud managed-registry users](ovhcloud_cloud_managed-registry_users.md)	 - Manage container registry users

