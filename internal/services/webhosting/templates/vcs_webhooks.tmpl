🔗 VCS Webhooks {{.ServiceName}}
==============================

**Path**: {{index .Result "path"}}

**Platform**: {{index .Result "vcs"}}

{{- with $push := index .Result "push" }}
**Push webhook**: {{$push}}
{{- end }}

💡 Use option -o json or -o yaml to get the raw output with all information
