🌍 Website {{.ServiceName}}
================

**ID**: {{index .Result "id"}}

**Path**: {{index .Result "path"}}

{{ with $displayName := index .Result "hostingDisplayName" -}}
**Hosting**: {{$displayName}}

{{- end }}

**VCS URL**: {{index .Result "vcsUrl"}}

**Branch**: {{index .Result "vcsBranch"}}

**Status**: {{index .Result "status"}}

{{ with $domains := index .Result "domains" -}}
**Domains**:
{{- range $domains }}
- {{index . "fqdn"}} (CDN: {{index . "cdn"}}, SSL: {{index . "ssl"}})
{{- end }}

{{- end }}

💡 Use option -o json or -o yaml to get the raw output with all information
