📈 CDN Statistics for {{.ServiceName}}
==============================

{{ $series := index .Result "series" -}}
{{ if not $series -}}
_No statistics available for this domain._
{{ else -}}
{{ range $serie := $series -}}
### {{index $serie "name"}} ({{index $serie "unit"}})
{{ $points := index $serie "points" -}}
{{ if $points }}
| Timestamp | Value |
| --- | --- |
{{ range $points -}}
| {{index . "timestamp"}} | {{index . "value"}} |
{{ end -}}
{{ else -}}
No datapoints recorded.
{{ end }}

{{ end -}}
{{ end }}

💡 Use option -o json or -o yaml to get the raw output with all information
