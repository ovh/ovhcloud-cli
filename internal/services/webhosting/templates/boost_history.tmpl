⚡️ Boost History {{.ServiceName}}
============================

{{ $entries := index .Result "entries" -}}
{{ if not $entries -}}
_No boost history recorded for this service._
{{ else -}}
{{ range $entry := $entries -}}
### 📅 {{index $entry "date"}}
- Offer: {{index $entry "offer"}}
{{ if index $entry "boostOffer" -}}- Boost offer: {{index $entry "boostOffer"}}{{ end }}
{{ if index $entry "accountId" -}}- Requested by: {{index $entry "accountId"}}{{ end }}

{{ end -}}
{{ end }}

💡 Use option -o json or -o yaml to get the raw output with all information
