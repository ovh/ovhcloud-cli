📋 SSL Report {{.ServiceName}}
================================

{{- $report := .Result }}
{{- if not $report }}
No SSL report available.
{{- else }}
- Domains covered: {{with (index $report "domains")}}{{range .}}{{.}} {{end}}{{else}}N/A{{end}}
- Date: {{with (index $report "date")}}{{.}}{{else}}N/A{{end}}
- Grade: {{with (index $report "grade")}}{{.}}{{else}}N/A{{end}}
{{- end }}

💡 Use option -o json or -o yaml to inspect the raw payload.
