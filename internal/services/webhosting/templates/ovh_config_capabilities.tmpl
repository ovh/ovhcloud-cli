🧱 OVH Config Capabilities {{.ServiceName}}
==================================

{{ $entries := index .Result "entries" -}}
{{ if $entries }}
| PHP Version | Container Images |
|-------------|------------------|
{{ range $entries -}}
| {{index . "version"}} | {{join (index . "containerImage")}} |
{{ end }}
{{ else }}
No capabilities available.
{{ end }}

💡 Use option -o json or -o yaml to get the raw output with all information
