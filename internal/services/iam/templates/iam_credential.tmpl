🔑 API credential {{ index .Result "credentialId" }}
=======

**Status**: {{ index .Result "status" }}
**Application**: {{ index .Result "applicationId" }}
**Created**: {{ index .Result "creation" }}
**Last used**: {{ with index .Result "lastUse" }}{{ . }}{{ else }}never{{ end }}
**Expires**: {{ with index .Result "expiration" }}{{ . }}{{ else }}never{{ end }}
**Usable from**: {{ with index .Result "allowedIPs" }}{{ . }}{{ else }}anywhere{{ end }}

## What this key may call

| Method | Path |
|---|---|
{{- range $rule := index .Result "rules" }}
| {{ index $rule "method" }} | {{ index $rule "path" }} |
{{- end }}

A rule on `/*` covers the whole API with that verb.

Revoke it with:
    ovhcloud iam credential delete {{ index .Result "credentialId" }}
